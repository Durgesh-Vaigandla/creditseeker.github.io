function calculate() {
    var inputNumber = document.getElementById("inputNumber").value;
    var result = (inputNumber )* 2 / 3;
    document.getElementById("result").innerHTML = "Result: " + result;
}